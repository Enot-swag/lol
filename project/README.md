<<<<<<< HEAD
# Итоговый проект по курсу «Инженерия Искусственного Интеллекта»

В этой папке находится итоговый мини-проект по курсу.  
Проект должен демонстрировать применение методов и инструментов инженерии ИИ: работу с данными, модели, пайплайны, сервис, эксперименты и (по возможности) воспроизводимость.

Заполните данный файл под ваш конкретный проект.

---

## 1. Паспорт проекта

- **Название проекта:** `Credit Scoring — предсказание дефолта по кредиту`
- **Автор:** `Еськина Ева Руслановна`
- **Группа:** `Группа БФБО-03-24 `
- **Контакт:** `@evka_Es`

- **Краткое описание (2-4 предложения):**  
  `<о чём проект, какую задачу решает, какие данные и методы используются>`

Пример:

>Проект посвящён построению сервиса предсказания вероятности дефолта по кредиту на основе финансовых и персональных характеристик клиента. Используется открытый датасет "Give Me Some Credit" (Kaggle, 150 000 записей), модели машинного обучения (логистическая регрессия, случайный лес). Результат – REST API сервис на FastAPI, который по входным параметрам клиента возвращает вероятность дефолта и рекомендацию (одобрить/отказать).

---

## 2. Структура проекта

Проект организован в следующей структуре:

project/
│
├── requirements.txt # зависимости проекта
├── .env.example # пример переменных окружения
├── .gitignore # игнорируемые файлы
├── README.md # документация
│
├── app.py # FastAPI сервис
├── run_eda.py # скрипт для EDA
├── run_training.py # скрипт для обучения моделей
│
├── src/ # основной код проекта
│ ├── init.py
│ ├── config.py # конфигурация
│ ├── data_preparation.py # загрузка и предобработка данных
│ └── eda.py # разведочный анализ данных
│
├── data/ # данные
│ └── cs-training.csv # датасет Give Me Some Credit
│
├── models/ # сохранённые модели (артефакты)
│ ├── best_model.pkl # лучшая модель
│ ├── scaler.pkl # стандартизатор
│ └── feature_names.pkl # имена признаков
│
├── tests/ # тесты
│ ├── init.py
│ ├── test_api.py # тесты API
│ └── test_preprocessing.py # тесты предобработки
│
├── docker/ # Docker файлы
│ └── Dockerfile
│
├── docs/ # документация и графики EDA
│ ├── target_dist.png # распределение целевой переменной
│ ├── distributions.png # распределения признаков
│ └── corr_matrix.png # матрица корреляций
│
└── docker-compose.yml # Docker Compose конфигурация

- Вместо `artifacts/` используется папка `models/` для сохранённых моделей
- Добавлены скрипты `run_eda.py` и `run_training.py` для удобного запуска
- `app.py` находится в корне для простого запуска API сервиса
- В `src/` только основные модули (без отдельного модуля для сервиса)

---

## 3. Требования и установка

### 3.1. Требования

- **Python** >= 3.10
- **pip** >= 23.0
- Рекомендуемый объём ОЗУ: 4GB+
- Docker (опционально, для контейнеризации)


### 3.2. Установка окружения

Примерный сценарий (можно адаптировать под ваш проект):

```bash
# Перейти в папку проекта
cd project

# Создать виртуальное окружение
python -m venv venv

# Активировать окружение:
# Windows:
venv\Scripts\activate
# Linux / macOS:
source venv/bin/activate

# Обновить pip
pip install --upgrade pip

# Установить зависимости
pip install -r requirements.txt

Особенности установки:
На Windows может потребоваться установка Microsoft Visual C++ Redistributable
При проблемах с pandas рекомендуется установить numpy перед pandas
Для ускорения установки можно использовать зеркало:

pip install -r requirements.txt -i https://mirror.yandex.ru/pypi/simple/
---

## 4. Как запустить проект

Опишите, как именно воспроизвести основные сценарии использования проекта.

Примеры (заполните под свой случай):

### 4.1. Запуск EDA (разведочный анализ данных)

```bash
cd project
venv\Scripts\activate  # Windows
python run_eda.py
```

### 4.2.  Запуск обучения модели

```bash
cd project
venv\Scripts\activate  # Windows
python run_training.py
```

### 4.3. Запуск API сервиса

```bash
cd project
venv\Scripts\activate  # Windows
python app.py
```

Или, если используется Docker:

```bash
cd project
docker-compose up --build
```



После запуска сервис доступен:
Адрес: http://localhost:8000
Swagger документация: http://localhost:8000/docs
Health check: http://localhost:8000/health

Основные эндпоинты:
Метод	Эндпоинт	Описание
GET	/	Информация о сервисе
GET	/health	Проверка работоспособности
POST	/predict	Предсказание дефолта
GET	/docs	Swagger UI документация

---

## 5. Данные

Кратко опишите используемые данные:
Источник: открытый датасет "Give Me Some Credit" с платформы Kaggle

Размер данных: 150 000 записей, 11 колонок

Структура:

10 признаков (возраст, доход, долговая нагрузка, просрочки и др.)

1 целевая переменная SeriousDlqin2yrs (дефолт: 1 / нет дефолта: 0)

Признаки:

RevolvingUtilizationOfUnsecuredLines — коэффициент использования непокрытых кредитных линий

age — возраст заёмщика

NumberOfTime30-59DaysPastDueNotWorse — количество просрочек 30-59 дней

DebtRatio — коэффициент долговой нагрузки

MonthlyIncome — ежемесячный доход

NumberOfOpenCreditLinesAndLoans — количество открытых кредитов

NumberOfTimes90DaysLate — количество просрочек более 90 дней

NumberRealEstateLoansOrLines — количество кредитов на недвижимость

NumberOfTime60-89DaysPastDueNotWorse — количество просрочек 60-89 дней

NumberOfDependents — количество иждивенцев

Где лежат файлы:

Полный датасет: data/cs-training.csv

Особенности данных:

Дисбаланс классов: 93.3% — нет дефолта, 6.7% — дефолт

Пропуски в колонках MonthlyIncome (19.8%) и NumberOfDependents (2.6%)

---

## 6. Тесты (если есть)

Описание тестов
Файл	Что тестирует
test_preprocessing.py	Обработку пропусков и выбросов
test_api.py	Эндпоинты /health и /predict

Пример:

```bash
cd project
venv\Scripts\activate  # Windows
pytest tests/ -v
```

Ожидаемый результат:

text
tests/test_preprocessing.py::test_handle_missing PASSED
tests/test_preprocessing.py::test_remove_outliers_iqr PASSED
tests/test_api.py::test_health PASSED
tests/test_api.py::test_predict PASSED

---

## 7. Демонстрация на защите

На защите я планирую показать:

Сценарий демонстрации:
1. Структура проекта 
- Покажу организацию кода: src/, models/, tests/, data/
- Поясню, за что отвечает каждый модуль

2. Результаты EDA 
- Покажу графики из docs/
- Продемонстрирую дисбаланс классов (6.7% дефолтов)
- Покажу матрицу корреляций

3. Результаты моделей 
- Представлю таблицу сравнения:
- Logistic Regression: ROC-AUC = 0.8036
- Random Forest: ROC-AUC = 0.7655
- Обосную выбор финальной модели (логистическая регрессия)

4. Запуск сервиса и проверка API 
- Запущу python app.py
- Открою Swagger UI (http://localhost:8000/docs)

5. Выполню тестовые запросы:
- Клиент с низким риском → низкая вероятность дефолта
- Клиент с высоким риском → высокая вероятность дефолта
- Health check и мониторинг 

6. Покажу эндпоинт GET /health

7. Покажу логи запросов в терминале

Ключевые метрики для демонстрации:
ROC-AUC = 0.8036
Время ответа API < 50 мс
Health check: {"status":"healthy","model_loaded":true}

---

## 8. Ограничения и дальнейшая работа

Текущие ограничения
Модели: обучены только 2 базовые модели (LogReg, Random Forest)

Гиперпараметры: не проводился подбор гиперпараметров (использованы значения по умолчанию)
MLflow: не настроен полноценно (требуется запуск отдельного сервера)
Мониторинг: нет Prometheus + Grafana
Нейросети: MLP модель не была дообучена (только заготовка в train_nn.py)
Направления дальнейшей работы

Улучшение моделей:
Добавить LightGBM и XGBoost
Подбор гиперпараметров с GridSearchCV / Optuna
Ансамблирование моделей (стэкинг)

Инфраструктура:
Настроить MLflow для отслеживания экспериментов
Добавить Prometheus метрики (количество запросов, время ответа)
Внедрить мониторинг дрейфа данных

Деплой:
Развернуть в облаке (AWS/GCP/Yandex Cloud)
Добавить CI/CD пайплайн (GitHub Actions)
Настроить авторизацию и rate limiting

Качество данных:
Feature engineering (создание новых признаков)
Балансировка классов (SMOTE, классовые веса)

API улучшения:
Добавить кэширование предсказаний
Реализовать асинхронную обработку batch-запросов
Добавить версионирование модели

=======
# Репозиторий студента по курсу «Инженерия Искусственного Интеллекта»

Этот репозиторий создан из шаблона и используется **одновременно** для:

- выполнения домашних заданий по семинарам;
- разработки и демонстрации итогового проекта.

Пожалуйста, не меняйте базовую структуру каталогов (`homeworks/` и `project/`), чтобы преподавателю было проще проверять работы.

---

## Информация о студенте

Заполните этот блок **перед началом работы**:

- ФИО: `Еськина Ева Руслановна`
- Группа: `БФБО-02-24`
- Контакт: `@evka_Es`
>>>>>>> 43e1a081e7c02fbb4f5921961d634c4847b59bd0


---

<<<<<<< HEAD
## 9. Оценка проекта

Итоговая оценка за проект выставляется по пятибалльной шкале (2-5).

Ориентиры для оценки:

- **2** – проект не принят:
  - не выполняются минимальные требования (сервис не запускается, отсутствует ключевой функционал);
  - грубые нарушения требований курса;
  - явный плагиат или формальная имитация работы.
- **3** – проект принят, но реализован на базовом уровне:
  - минимальный функционал есть;
  - по чеклисту `self-checklist.md` выполнено **меньше 5** пунктов;
  - эксперименты и документация слабо проработаны.
- **4** – хороший, рабочий проект:
  - сервис запускается по `README.md`, `/predict` использует реальную модель;
  - есть данные, EDA и эксперименты с метриками;
  - структура кода и конфигураций в целом адекватна;
  - по чеклисту выполнено **не менее 5** пунктов.
- **5** – сильный, хорошо проработанный проект:
  - аккуратно реализован сервис и пайплайн;
  - проведены осмысленные эксперименты и обоснован выбор финальной модели;
  - есть базовая наблюдаемость и работа с конфигами/секретами;
  - документация позволяет быстро понять и воспроизвести решение;
  - по чеклисту выполнено **не менее 9** пунктов.

Чеклист `self-checklist.md` служит для самопроверки студента и как подсказка при проверке.  
Окончательное решение по оценке остаётся за преподавателем и может учитывать:

- качество реализации внутри каждого пункта чеклиста;
- дополнительные сильные стороны проекта (нестандартные решения, дополнительные функции, продвинутая ML-часть и т.п.);
- соблюдение требований курса и дедлайнов.
=======
## Структура репозитория

В репозитории предусмотрены две основные области:

- `homeworks/` – домашние задания по семинарам;
- `project/` – итоговый мини-проект.

Подробнее:

- `homeworks/`
  - `README.md` – краткие правила оформления домашних заданий.
  - `HW01/`, `HW02/`, `HW03/`, ... – отдельная папка под каждое задание.
    Для каждого задания должен быть **один основной файл** с таким же номером, например:
    - `homeworks/HW01/HW01.ipynb`
    - `homeworks/HW02/HW02.ipynb`
    - и т.д.

- `project/`
  - `README.md` – паспорт проекта и инструкции по запуску.
  - `report.md` – отчёт по проекту (постановка задачи, данные, эксперименты, результаты).
  - `self-checklist.md` – чеклист самопроверки проекта перед сдачей.
  - `requirements.txt` – зависимости **только для проекта**.
  - `notebooks/` – экспериментальные ноутбуки, EDA, прототипы.
  - `src/` – основной код проекта (модули, пайплайны, сервисы).
  - `data/` – демонстрационные/учебные данные (без персональных и конфиденциальных данных).
  - `configs/` – конфигурационные файлы, шаблоны `.env.example` и т.п.
  - `tests/` – тесты (если используются).
  - `artifacts/` – сохранённые модели, отчёты, артефакты обучения.

---

## Как работать с домашними заданиями

1. Для каждого нового задания создавайте папку:
   - `homeworks/HW01/`
   - `homeworks/HW02/`
   - `homeworks/HW03/`
   - и т.д.

2. Основной файл задания должен называться:
   - `HW01.ipynb` в папке `HW01/`;
   - `HW02.ipynb` в папке `HW02/`;
   - и т.п.

3. При необходимости вы можете добавлять дополнительные файлы в соответствующую папку (`.py`, дополнительные ноутбуки, вспомогательные данные), если это не противоречит требованиям задания.

4. **Не переименовывайте** папку `homeworks/` и папки формата `HWNN/` (например, `HW01/`, `HW02/` и т.п.), чтобы не ломать автоматическую и визуальную проверку.

---

## Как работать над проектом

- Весь код и материалы проекта размещайте **только** в папке `project/`.
- В файле `project/README.md` опишите:
  - цель проекта;
  - краткую идею;
  - как запустить проект (команды, зависимости, параметры);
  - как воспроизвести демонстрацию для защиты.
- Отдельный отчёт по проекту ведите в файле `project/report.md`:
  - постановка задачи и метрики;
  - описание данных и экспериментов;
  - сравнение моделей и выбор финальной модели.
- Для самопроверки перед сдачей используйте чеклист в `project/self-checklist.md`.
- Все зависимости проекта фиксируйте в `project/requirements.txt`.

---

## Оценка итогового проекта

Итоговая оценка за проект выставляется по пятибалльной шкале (2–5) и зависит от:

- соблюдения минимальных требований к работоспособности сервиса и структуре проекта;
- количества выполненных пунктов чеклиста в `project/self-checklist.md`;
- качества отчёта `project/report.md` и общей аккуратности реализации.

Ориентировочно:

- если проект не выполняет минимальные требования (не запускается, отсутствует ключевой функционал, грубые нарушения, явный плагиат) – оценка **2**;
- если минимум пройден, но по чеклисту выполнено **меньше 5** пунктов – оценка **3**;
- если по чеклисту выполнено **не менее 5** пунктов – оценка **4**;
- если по чеклисту выполнено **не менее 9** пунктов – оценка **5**.

Окончательное решение по оценке остаётся за преподавателем и может учитывать дополнительные сильные стороны проекта и соблюдение дедлайнов.

---

## Безопасность и работа с данными

- Не коммитьте в репозиторий пароли, токены, ключи, файлы с реальными персональными данными третьих лиц и закрытые датасеты.
- Если в проекте нужны данные, используйте:
  - открытые датасеты;
  - синтетические данные;
  - обезличенные/анонимизированные выборки.

Подробные правила и ограничения описаны в файле [`SECURITY.md`](./SECURITY.md). Пожалуйста, ознакомьтесь с ним перед началом работы.
>>>>>>> 43e1a081e7c02fbb4f5921961d634c4847b59bd0

---
